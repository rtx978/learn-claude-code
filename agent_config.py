PROMPT_CPP = "Generate C++ competition template with standard headers, input/output handling, and code structure. Output only the code without explanations.\n\n// Example template:\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    // Input handling\n    int n; cin >> n;\n    vector<int> nums(n);\n    for (auto &x: nums) cin >> x;\n    \n    // Processing\n    sort(nums.begin(), nums.end());\n    \n    // Output\n    for (auto x: nums) cout << x << " ";\n    return 0;\n}"\nPROMPT_JAVA = "Generate Java competition template with class structure, main method, and input/output handling. Output only the code without explanations.\n\n// Example template:\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[] nums = new int[n];\n        for (int i=0; i<n; i++) nums[i] = sc.nextInt();\n        \n        // Sorting\n        Arrays.sort(nums);\n        \n        // Output\n        for (int x: nums) System.out.print(x + " ");\n    }\n}"